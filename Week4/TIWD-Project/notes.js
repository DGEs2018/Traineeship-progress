let { Datetime } = require 'luxon';
function printHello() {
    console.log('Hello, World!');
    console.log(luxon.Datetime.local())
}

setTimeout(printHello, 2000);

console.log(document.addEventListener);
const reverse = function() {}

const reverse = ()=> {

}

const word = 'hola';

const characters = word.split('');
console.log(characters);
const reversedCharacters = characters.reverse();
const result = reverseCharacters.join('');

value of the function would be at the call of function
//manually iterate through all the characters

let result = '';
for(let i=0;/*initializer only called once*/ i < 10; i++) {
    let character = word[i];
    result = result + character;
    console.log(result);
}

let result = '';
for(let i = word.length;/*initializer only called once*/ i >= 0; i = i+1) {
    console.log('before da block');
    for (let j= word.length-1; j >= 0; j = j-1)
    let character = word[i];
    result = result + character;
    console.log(result);
}


